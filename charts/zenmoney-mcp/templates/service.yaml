apiVersion: v1
kind: Service
metadata:
  name: {{ include "zenmoney-mcp.fullname" . }}
  labels:
    {{- include "zenmoney-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "zenmoney-mcp.selectorLabels" . | nindent 4 }}
